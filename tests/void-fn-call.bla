def main() {
	#? STDOUT 6
	f(1, 2, 3);

	# now build an array containing the function pointer.
	#? STDOUT 15
	arr = [] + &f;
	arr[0](4,5,6);

	#and objects as well
	#? STDOUT 12
	obj = {};
	obj.myfunc = &f;
	obj.myfunc(10, 1, 1);
}


def f(x, y, z) {
	print(x+y+z);
}
